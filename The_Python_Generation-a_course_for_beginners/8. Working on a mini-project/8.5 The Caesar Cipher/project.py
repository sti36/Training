"""Аве, Цезарь 🌶️
На вход программе подаётся строка текста на английском языке, в которой нужно зашифровать все слова. Каждое слово строки следует зашифровать с помощью шифра Цезаря (циклического сдвига на длину этого слова). Строчные буквы при этом остаются строчными, а прописные – прописными. Гарантируется, что между различными словами присутствует один пробел.

Формат входных данных 
На вход программе подаётся строка текста на английском языке.

Формат выходных данных
Программа должна вывести зашифрованный текст в соответствии с условием задачи.

Примечание. Символы, не являющиеся английскими буквами, не изменяются."""

def caesar_cipher(text):
    # Вспомогательная функция для смещения одиночного символа
    def shift_char(c, n):
        '''Функция для сдвига одного символа'''
        if c.islower():     # Если символ строчный
            base = ord('a') # Используем базовый индекс 'a'
        elif c.isupper():   # Если символ заглавный
            base = ord('A') # Используем базовый индекс 'A'
        else:               # Если символ не буква
            return c        # Возвращаем его без изменений
        # Вычисляем новый символ с учётом сдвига
        return chr((ord(c) - base + n) % 26 + base)

    # Разделение текста на отдельные слова
    words = text.split()
    result = []              # Здесь собираем зашифрованные слова

    # Проходим по каждому слову
    for word in words:
        # Отфильтровываем только буквы (игнорируя знаки препинания)
        letters_only = ''.join(filter(str.isalpha, word))
        # Определяем сдвиг как длину фильтрованного слова
        shift = len(letters_only)
        
        # Формируем зашифрованное слово, оставляя специальные символы без изменения
        encrypted_word = ''.join(
            [shift_char(ch, shift) if ch.isalpha() else ch for ch in word]
        )
        result.append(encrypted_word)  # Добавляем зашифрованное слово в результат

    # Объединяем зашифрованные слова обратно в строку
    return ' '.join(result)

# Тестируем нашу реализацию
input_text = input()
output_text = caesar_cipher(input_text)
print(output_text)